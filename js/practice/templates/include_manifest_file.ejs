<div id="include_manifest_file">
	<div id="header">
		<div class="topic"><%= title %></div>
	</div>
	<div class="content">
		<% _.each(content.description, function (item) { %>
			<div class="<%= _.keys(item) %>"><%= _.values(item) %></div>
		<% }); %>
		<% _.each(content.html, function (item) { %>
			<div class="<%= _.keys(item) %>"><%= _.values(item) %></div>
		<% }); %>
		<div class="htmlNote_container">
			<% _.each(content.htmlNote, function (item) { %>
				<div class="<%= _.keys(item) %>"><%= _.values(item) %></div>
			<% }); %>
		</div>
	</div>
	<div id="orange_line"></div>
	<div id="blue_line_bottom"></div>
	<div id="footer"></div>
</div>